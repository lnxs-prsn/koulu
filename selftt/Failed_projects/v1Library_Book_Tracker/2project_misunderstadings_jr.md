__init__()
    -   runs only once 
    -   attributes needs to be changed manually after initialization 
        -   preferably within a method 
            -   so the coordinator can tell the object to update the attribute values


