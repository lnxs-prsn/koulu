

Please write a function named column_correct(sudoku: list, column_no: int), which takes a two-dimensional array representing a sudoku grid, and an integer referring to a single column, as its arguments. Columns are indexed from 0.
The function should return True or False, depending on whether the column is filled in correctly, that is, whether it contains each of the numbers 1 to 9 at most once.
LOGICAL OPERATIONS 
    -   init func
    -   take list and num
    -   init empty list
    -   init loop
    -   iterate lists by num as inner list index index
    -   check if element not 0 and not in the list
    -   append element to list
    -   else return false
    -   return true
1. yes
   1. check if element not 0 and not in the list
   2. append element to list
2. yes
   1. check if element not 0 and not in the list
   2. append element to list
   3. else return false
3. yes
   1. check if element not 0 and not in the list
   2. append element to list
   3. else return false
4. yes
   1. there but there is accumulation to track the dublicates
5. yes
   1. check if element not 0 and not in the list
   2. append element to list
   3. else return false
6. yes
   1. is there dublicates in the list
7. yes
   1. check if x not y and not in the z
   2. append x to z
   3. else return false