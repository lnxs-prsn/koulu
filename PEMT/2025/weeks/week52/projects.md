25.12(
p1 algorithm:
    -   patterns recognition
        -   task unfinished not evaluated

p2 OOP principles 
    -   task not finished
)


26.12(
p1 algorithm
    -   pattern recognition
        -   task unfinished not evaluated
        -   learning process is really slow
        -   reason jummah day and p1 goal is too unrealistic new template leads to very slow progress in the beginning
p2 OOP principles
    -   using the template 
    -   very slow progress 
    -   I think the goal of the p2 is too unrealistic needs to be adjusted

)


27.12(
    off
)

28.12(
    off
)

